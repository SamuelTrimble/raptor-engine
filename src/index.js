import { DEBUGLEVEL, RaptorEngine } from './scripts/re.main.js'

window.STL = (window.STL || {});
window.STL.DEBUGLEVEL = (window.STL.DEBUGLEVEL || DEBUGLEVEL);
window.STL.RaptorEngine = (window.STL.RaptorEngine || RaptorEngine);
